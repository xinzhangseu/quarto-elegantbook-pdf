$if(title)$
\title{$title$}
$endif$

$if(subtitle)$
\subtitle{$subtitle$}
$endif$

$if(by-author)$
\author{$for(by-author)$$author.tex()$$sep$\hspace{2em}$endfor$}
$for(by-author)$$if(it.attributes.corresponding)$$if(it.email)$
\AtBeginDocument{\bioinfo{E-mail}{\texttt{$it.email$}}}
$endif$$endif$$endfor$
$endif$

$if(date)$
\date{$date$}
$endif$
